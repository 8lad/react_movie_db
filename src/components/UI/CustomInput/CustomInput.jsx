import React from "react";
import TextField from "@mui/material/TextField";
const Input = (props) => {
  return <TextField variant="outlined" margin="dense" fullWidth {...props} />;
};

export default Input;
